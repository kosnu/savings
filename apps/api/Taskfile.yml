# https://taskfile.dev

version: "3"

tasks:
  default:
    aliases:
      - list
    desc: List all tasks
    cmd: task -l

  up:
    desc: Start Supabase and serve functions locally
    cmds:
      - task start:supabase
      - task serve:functions

  down:
    desc: Stop Supabase and functions
    cmds:
      - task stop:functions
      - task stop:supabase

  start:supabase:
    desc: Start Supabase locally
    cmd: npx supabase start

  stop:supabase:
    desc: Stop Supabase locally
    cmd: npx supabase stop

  serve:functions:
    desc: Serve Supabase Edge Functions locally
    cmds:
      - mkdir -p .run
      - sh -c 'nohup npx supabase functions serve > .run/functions.log 2>&1 & echo $! > .run/functions.pid'
  stop:functions:
    desc: Stop background Supabase Edge Functions
    cmds:
      - sh -c 'test -f .run/functions.pid && kill "$(cat .run/functions.pid)" || true'
      - rm -f .run/functions.pid

  logs:functions:
    desc: Tail functions logs
    cmd: tail -f .run/functions.log

  up:migrations:
    desc: Run database migrations to latest
    cmd: npx supabase migration up

  down:migrations:
    desc: Rollback the last database migration
    cmd: npx supabase migration down

  test:
    desc: Run tests for all functions
    cmds:
      - task: test:payments
      - task: test:categories

  test:payments:
    desc: Run tests for payments function
    cmd: deno test --allow-read --allow-env .
    dir: "{{.TASKFILE_DIR}}/supabase/functions/payments"

  test:categories:
    desc: Run tests for categories function
    cmd: deno test --allow-read --allow-env .
    dir: "{{.TASKFILE_DIR}}/supabase/functions/categories"

  lint:
    desc: Run deno lint for all functions
    cmds:
      - task: lint:payments
      - task: lint:categories

  lint:payments:
    desc: Run deno lint for payments function
    cmd: deno lint
    dir: "{{.TASKFILE_DIR}}/supabase/functions/payments"

  lint:categories:
    desc: Run deno lint for categories function
    cmd: deno lint
    dir: "{{.TASKFILE_DIR}}/supabase/functions/categories"

  fmt:
    desc: Run deno fmt for all functions
    cmds:
      - task: fmt:payments
      - task: fmt:categories

  fmt:payments:
    desc: Run deno fmt for payments function
    cmd: deno fmt
    dir: "{{.TASKFILE_DIR}}/supabase/functions/payments"

  fmt:categories:
    desc: Run deno fmt for categories function
    cmd: deno fmt
    dir: "{{.TASKFILE_DIR}}/supabase/functions/categories"

  check:
    desc: Run type check for all functions
    cmds:
      - task: check:payments
      - task: check:categories

  check:payments:
    desc: Run type check for payments function
    cmd: deno check --config deno.json **/*.ts
    dir: "{{.TASKFILE_DIR}}/supabase/functions/payments"

  check:categories:
    desc: Run type check for categories function
    cmd: deno check --config deno.json **/*.ts
    dir: "{{.TASKFILE_DIR}}/supabase/functions/categories"

  verify:
    desc: Run type check, lint, and fmt check
    cmds:
      - task: check
      - task: fmt
      - task: lint
