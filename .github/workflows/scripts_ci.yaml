name: Scripts CI

on:
  pull_request:
    paths:
      - "!**/docs/**"
      - ".github/workflows/scripts_ci.yaml"
      - "scripts/**"

jobs:
  import_to_firestore_ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: scripts/import_to_firestore
    steps:
      - uses: actions/checkout@v5

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Check
        run: deno check

      - name: Lint
        run: deno lint

      - name: Test
        run: deno test --allow-read
