interface:
  display_name: "AIDD Cycle"
  short_description: "Run one complete AIDD workflow with sequential Goals"
  default_prompt: "Use $aidd-cycle to run one complete cycle according to the repository AI Driven Development workflow."

policy:
  allow_implicit_invocation: true
