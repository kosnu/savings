interface:
  display_name: "Context Scout"
  short_description: "Find evidence for a bounded delegated repository question"
  default_prompt: "Use $context-scout with the context-scout custom agent for this bounded delegated question and return concise findings with file references."

policy:
  allow_implicit_invocation: true
