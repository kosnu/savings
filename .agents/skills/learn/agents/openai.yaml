interface:
  display_name: "Learn"
  short_description: "Improve reusable guardrails without product changes"
  default_prompt: "Use $learn to analyze the specified feedback."

policy:
  allow_implicit_invocation: true
