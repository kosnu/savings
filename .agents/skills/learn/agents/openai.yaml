interface:
  display_name: "Learn"
  short_description: "Route learnings to task context or precise rules"
  default_prompt: "Use $learn to route each finding to task context, a rule change, or rule sharpening."

policy:
  allow_implicit_invocation: true
