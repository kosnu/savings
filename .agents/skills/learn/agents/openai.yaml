interface:
  display_name: "Learn"
  short_description: "Check reachability and select preventive learning"
  default_prompt: "Use $learn to exclude findings unreachable through supported normal paths, investigate applicable causes, compare preventive countermeasures, and route selected learning to its canonical owner."

policy:
  allow_implicit_invocation: true
