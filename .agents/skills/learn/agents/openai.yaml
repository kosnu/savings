interface:
  display_name: "Learn"
  short_description: "Apply the canonical learning-extraction policy"
  default_prompt: "Use $learn to process the eligible findings according to the repository's canonical learning-extraction policy."

policy:
  allow_implicit_invocation: true
