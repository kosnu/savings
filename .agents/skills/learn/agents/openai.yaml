interface:
  display_name: "Learn"
  short_description: "Investigate causes and select preventive learning"
  default_prompt: "Use $learn to investigate each finding's cause, compare preventive countermeasures, and route the selected learning to its canonical owner."

policy:
  allow_implicit_invocation: true
