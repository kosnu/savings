interface:
  display_name: "Learn"
  short_description: "Extract learnings into task context or rules"
  default_prompt: "Use $learn to extract reusable learning from feedback and route it into task context or durable rules."

policy:
  allow_implicit_invocation: true
