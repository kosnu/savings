interface:
  display_name: "Learn"
  short_description: "Route learnings to context, rules, or policies"
  default_prompt: "Use $learn to route each finding to task context, a rule or policy change, or rule or policy sharpening."

policy:
  allow_implicit_invocation: true
