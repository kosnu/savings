---
title: Transaction Boundaries
doc_type: policy
status: accepted
area: repository
applies_to:
  - apps/web
  - apps/api
topics:
  - transaction
  - consistency
  - rpc
  - api
  - database
when_to_read:
  - 複数のデータ作成・更新・削除を1つのユーザー操作で扱うとき
  - 片方だけ成功する状態を避ける必要があるとき
  - 書き込み後のread-backまたはrefetch失敗を扱うとき
  - RPC、API、DB transaction、アプリケーション層の責務境界を決めるとき
---

# Transaction Boundaries

1つのユーザー操作が複数のデータ作成・更新・削除を含む場合は、片方だけ成功した状態がユーザー体験やデータ整合性を壊さないかを確認します。

片成功を避ける必要がある場合は、Design / Planで同一操作単位を明示します。実装手段は既存構成と制約に合わせて選びます。

## 判断基準

次のいずれかに当てはまる場合は、同一操作単位として扱うことを検討します。

- 片方だけ成功すると、ユーザーに成功状態を誤認させる
- 片方だけ成功すると、後続画面や集計結果が要件と矛盾する
- ユーザーが1つの保存・作成・削除操作として認識する
- 失敗時に個別復旧よりも全体失敗として扱う方が自然
- 片成功の検知や補償処理が複雑になる

## Design / Planで決めること

- どのデータ変更を同一操作単位に含めるか
- 期間や履歴を持つデータでは、対象月や対象日を決める入力を同一操作単位に含めるか
- どの層で境界を作るか
- 操作の実行境界と、入力値へ適用するルールの所有境界をどの層が担うか
- 片成功が起きた場合のユーザー影響
- 採用しない案と理由
- 残る競合リスクやスコープ外の補償

## Write後の確認失敗

source of truthへのwriteと、その後のread-backまたはrefetchは、別の成功・失敗境界として扱います。write契約が対象の更新成功を確認している場合、後続の内部的なcache収束が失敗してもwrite失敗へ読み替えず、write前の値へ戻したように見せてはいけません。

Design / Planでは、少なくとも次の状態を区別し、どの境界が収束を所有するかを定義します。

- write失敗
- write成功・後続のcache収束が未完了または失敗
- write成功・後続のcache収束が完了

write成功後のread-backまたはrefetchが、ユーザー操作の結果確認ではなく内部的なcache収束だけを目的とする場合、その失敗をユーザー向けの未確認状態、失敗通知、または再試行操作へ昇格させません。cacheを所有する境界が、invalidation、内部retry、次回取得などによる収束と、収束していない状態の内部観測を所有します。

write契約だけでは保存結果そのものを確認できない場合、または後続のread-back失敗によってユーザー向け結果を正しく保てない場合に限り、未確認の結果としてユーザー操作の完了境界から観測可能にします。ユーザー操作が必要なら状態と再確認経路を伝え、内部処理だけで収束できる場合はユーザーへ操作を要求しません。

収束処理を再試行する場合は、原則としてread-backまたはrefetchを実行します。成功済みのwriteは、冪等性が保証され、その再実行を明示的に採用している場合を除き、自動で繰り返してはいけません。

検証では、writeが重複して実行されないこと、内部的なcache収束失敗が確認済みwriteの結果を覆さないこと、収束していないcacheを正本として固定しないこと、および採用した内部処理または必要なユーザー操作によって収束できることを確認します。

## 実行境界とルール所有境界

操作の原子性を保証する実行境界と、validation、正規化、上限、既定値などのルール所有境界は分けて判断します。

RPCやdatabase functionを実行境界に選んでも、その操作に関わるすべてのルールをDBが所有することにはなりません。ルールを配置する層は、その実行手段ではなく、ルールを守る理由と適用範囲で決めます。

- 呼び出し元に関係なく守る必要があり、データ整合性、参照整合性、認証、権限、または原子性を保護するルールは、APIまたはDBの境界で保証します。
- 特定のアプリケーションやユーザー体験に固有で、保存層がより広い値を正しく保持できる入力制約、正規化、上限、既定値は、そのアプリケーション層が所有します。
- アプリケーション層が所有する値ルールは、適用後の値をRPCやAPIへ明示的な入力として渡します。RPCやDB側で同じルールを再定義しません。
- 実行境界を選ぶ都合だけでルール所有境界を移す必要がある場合は、Design / Planで理由と影響範囲を明示します。根拠をRequirementsまたは正本ルールへ追跡できない場合はStopします。

## 実装境界の候補

- DB transaction / database function
- RPC / API endpoint
- アプリケーション層の明示的な更新順序と失敗処理
- 補償処理または再試行

DB constraint、trigger、RPC、API追加などが既存の制約やスコープを超える場合は、Build / Verifyで勝手に追加せずStop条件として扱います。

時系列状態を保存する操作では、DB側の現在日時で対象期間を補完して、read側とwrite側の期間基準を分離しないようにします。
