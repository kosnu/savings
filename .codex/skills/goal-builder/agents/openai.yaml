interface:
  display_name: "Goal Builder"
  short_description: "Build a Codex Goal from AI Driven Development templates"
  default_prompt: "Use $goal-builder to generate a ready-to-run Codex Goal for intent, design, build, ship, or the next phase."

policy:
  allow_implicit_invocation: true
